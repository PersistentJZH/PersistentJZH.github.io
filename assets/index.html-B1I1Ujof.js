import{a as t,c as i,f as s,o as a}from"./app-BajsgVfI.js";const n={};function l(p,e){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h2 id="一句话理解" tabindex="-1"><a class="header-anchor" href="#一句话理解"><span>一句话理解</span></a></h2><p>Kubernetes 原生 checkpoint 解决的是“把某个正在运行的容器运行现场保存下来”的问题，而不是完整解决“Pod 可靠恢复”“业务容灾”或“零中断迁移”的问题。</p><p>它的底层依赖 CRIU，因此能力边界也继承了 CRIU 的边界：CRIU 能保存很多 Linux 进程状态，比如内存页、寄存器、文件描述符、进程树、namespace 等；但它不能自动理解 Kubernetes Pod 语义、业务事务语义、远端连接语义、存储一致性和调度约束。</p><p>可以把它理解成：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>kubelet checkpoint API：</span></span>
<span class="line"><span>  负责触发某个容器的 checkpoint</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CRI Runtime + OCI Runtime + CRIU：</span></span>
<span class="line"><span>  负责把进程运行状态 dump 成 checkpoint archive</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Kubernetes 控制面：</span></span>
<span class="line"><span>  当前并没有提供一个稳定统一的 PodRestore API</span></span></code></pre></div><p>所以它很适合做故障取证、冷启动优化实验、特定单容器 workload 的状态保存；但如果把它直接当成“Pod 热迁移”或“通用容灾恢复”，就会遇到很多边界问题。</p><h2 id="原生能力到底到哪一层" tabindex="-1"><a class="header-anchor" href="#原生能力到底到哪一层"><span>原生能力到底到哪一层</span></a></h2><p>截至 2026-05-29，Kubernetes <code>ContainerCheckpoint</code> feature gate 在 v1.30 起是 Beta 且默认开启。它暴露的是 kubelet 上的节点本地接口：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>POST /checkpoint/{namespace}/{pod}/{container}</span></span></code></pre></div><p>这个接口有几个关键特征：</p><ol><li><strong>它是 kubelet API，不是 apiserver 上的普通 Kubernetes 资源 API</strong>。</li><li><strong>它的对象是 container，不是 Pod</strong>。</li><li><strong>它只负责 checkpoint，不提供稳定统一的 restore API</strong>。</li><li><strong>checkpoint archive 的具体内容依赖底层 CRI Runtime 实现</strong>。</li><li><strong>如果 CRI Runtime 没有实现 checkpoint，kubelet 会返回 500</strong>。</li></ol><p>一次请求的大致链路是：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>用户 / 工具</span></span>
<span class="line"><span>  -&gt; kubelet checkpoint API</span></span>
<span class="line"><span>  -&gt; CRI Runtime</span></span>
<span class="line"><span>  -&gt; OCI Runtime</span></span>
<span class="line"><span>  -&gt; CRIU</span></span>
<span class="line"><span>  -&gt; /var/lib/kubelet/checkpoints/checkpoint-*.tar</span></span></code></pre></div><p>恢复时通常不是再调用 kubelet 的某个 <code>restore</code> 接口，而是把 checkpoint archive 转成带 annotation 的 OCI image，再创建一个使用该 image 的 Pod，让 runtime 在创建容器时识别它是 checkpoint image，然后走 <code>runc restore</code> / <code>crun restore</code> / CRIU restore 路径。</p><p>这意味着 Kubernetes 原生 checkpoint 更像是一个“节点级容器状态导出能力”，而不是完整的声明式“Pod 状态生命周期管理能力”。</p><h2 id="局限一-它是容器级-不是-pod-级" tabindex="-1"><a class="header-anchor" href="#局限一-它是容器级-不是-pod-级"><span>局限一：它是容器级，不是 Pod 级</span></a></h2><p>Kubernetes 的调度单位是 Pod，但 kubelet checkpoint API 的操作对象是 Pod 里的某个 container。</p><p>单容器 Pod 里，这个问题不明显；多容器 Pod 里，这个限制会迅速放大。例如：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>Pod</span></span>
<span class="line"><span>├── app container</span></span>
<span class="line"><span>├── envoy / istio-proxy sidecar</span></span>
<span class="line"><span>├── log sidecar</span></span>
<span class="line"><span>└── metrics sidecar</span></span></code></pre></div><p>如果只 checkpoint <code>app container</code>，restore 后可能出现：</p><ol><li>app 的内存状态来自旧时间点。</li><li>sidecar 是重新启动的新进程。</li><li>app 和 sidecar 之间的 Unix socket、localhost TCP 连接、iptables 状态不一致。</li><li>mTLS 证书、短期 token、连接池状态已经变化。</li><li>共享 volume 中的文件状态和 app 内存中的状态不匹配。</li></ol><p>这不是 CRIU 单独能解决的问题。CRIU 可以保存一个进程树，但它不知道 Kubernetes 里哪些容器共同构成一个业务一致性边界。</p><p>如果要做 Pod 级 restore，需要额外实现：</p><ol><li>多容器一致性屏障。</li><li>checkpoint 前摘流和冻结顺序。</li><li>多个 container checkpoint 的版本绑定。</li><li>sidecar 是恢复还是重启的策略。</li><li>restore 后 readiness、流量切换和失败回滚。</li></ol><p>因此 Kubernetes 原生 checkpoint 只能作为 Pod 级能力的底层积木，不能直接等同于 Pod checkpoint。</p><h2 id="局限二-它没有统一的-kubernetes-restore-语义" tabindex="-1"><a class="header-anchor" href="#局限二-它没有统一的-kubernetes-restore-语义"><span>局限二：它没有统一的 Kubernetes Restore 语义</span></a></h2><p>Checkpoint 和 restore 是一对动作，但 Kubernetes 当前原生接口只把 checkpoint 暴露成 kubelet API。</p><p>这会带来几个直接问题：</p><ol><li>没有 <code>PodRestore</code> 这类内置资源。</li><li>没有标准 status 字段描述 restore 进度。</li><li>没有统一的失败原因、重试语义和回滚语义。</li><li>没有调度器原生理解“这个 Pod 必须恢复到兼容节点”的约束。</li><li>没有统一的 checkpoint archive 存储、加密、保留和 GC 策略。</li></ol><p>常见的 Kubernetes 内 restore 方式是：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>checkpoint archive</span></span>
<span class="line"><span>  -&gt; checkpointctl / buildah</span></span>
<span class="line"><span>  -&gt; checkpoint OCI image</span></span>
<span class="line"><span>  -&gt; push registry</span></span>
<span class="line"><span>  -&gt; create Pod with checkpoint image</span></span>
<span class="line"><span>  -&gt; runtime detects annotation</span></span>
<span class="line"><span>  -&gt; CRIU restore</span></span></code></pre></div><p>这个流程可以跑通，但它不是 Kubernetes 内置的声明式生命周期。它依赖 runtime 是否识别 checkpoint image annotation，也依赖 checkpoint image 的构建工具、镜像仓库和节点 runtime 版本。</p><p>结果就是：checkpoint 创建是“原生的”，restore 落地通常是“运行时特定 + 工具链拼装 + 平台自研控制器”。</p><h2 id="局限三-criu-不能保存所有-linux-资源" tabindex="-1"><a class="header-anchor" href="#局限三-criu-不能保存所有-linux-资源"><span>局限三：CRIU 不能保存所有 Linux 资源</span></a></h2><p>CRIU 很强，但它不是魔法。它只能保存内核暴露出来、并且 CRIU 已经支持建模的状态。</p><p>下面这些资源经常会成为 checkpoint 失败或 restore 异常的原因：</p><table><thead><tr><th style="text-align:left;">类型</th><th style="text-align:left;">典型问题</th></tr></thead><tbody><tr><td style="text-align:left;">特殊设备</td><td style="text-align:left;">GPU、RDMA、DPDK、FUSE、某些字符设备或块设备状态无法通用保存</td></tr><tr><td style="text-align:left;">调试状态</td><td style="text-align:left;">进程正在被 gdb、strace 等 ptrace 工具跟踪时，CRIU 很可能无法 dump</td></tr><tr><td style="text-align:left;">非常规 socket</td><td style="text-align:left;">CRIU 对 TCP、UDP、Unix、packet、netlink 等有支持边界，其他 socket 类型可能失败</td></tr><tr><td style="text-align:left;">文件锁</td><td style="text-align:left;"><code>flock</code> / <code>fcntl</code> 锁需要特殊处理，而且只有在锁相关进程都被纳入 dump 时才更安全</td></tr><tr><td style="text-align:left;">外部 Unix socket</td><td style="text-align:left;">如果对端进程不在 checkpoint 范围内，需要特殊选项或应用能容忍重连</td></tr><tr><td style="text-align:left;">被删除或不可见文件</td><td style="text-align:left;">进程打开了已经 unlink 或路径不可达的文件时，restore 可能找不到对应对象</td></tr><tr><td style="text-align:left;">IPC 对象</td><td style="text-align:left;">SysV IPC、共享内存等如果没有完整 namespace 视角，可能无法一致恢复</td></tr><tr><td style="text-align:left;">图形 / 桌面程序</td><td style="text-align:left;">状态可能存在外部 X server、Wayland compositor 或设备中</td></tr></tbody></table><p>在 Kubernetes 里，这些问题会被进一步放大。因为很多资源不是容器自己独占的，而是由 kubelet、CNI、CSI、device plugin、service mesh 或宿主机共同管理。</p><p>例如 GPU workload 里，进程状态在 CPU 内存中只是一部分，CUDA context、GPU 显存、驱动内部状态也很关键。普通 CRIU checkpoint 不会自动把这些状态完整保存下来。即使有专门的 GPU checkpoint 研究或厂商能力，也不能直接推导为 Kubernetes 原生 checkpoint 已经通用支持 GPU restore。</p><h2 id="局限四-网络连接很难透明迁移" tabindex="-1"><a class="header-anchor" href="#局限四-网络连接很难透明迁移"><span>局限四：网络连接很难透明迁移</span></a></h2><p>CRIU 支持一定程度的 TCP connection restore，但这不等于 Kubernetes 里可以透明迁移所有连接。</p><p>TCP 连接的恢复至少涉及：</p><ol><li>原本的本地 IP 和端口是否还能在目标节点使用。</li><li>对端是否还认为连接有效。</li><li>TCP sequence number、window、timestamp 等状态是否能正确恢复。</li><li>迁移期间包是否被丢弃、缓存或阻断。</li><li>iptables、conntrack、eBPF、CNI 路由是否保持一致。</li></ol><p>CRIU 的 live migration 文档也强调，restore TCP socket 时会尝试使用原来的地址信息；如果目标侧没有对应 IP，相关系统调用会失败。</p><p>放到 Kubernetes 中，问题更复杂：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>源 Pod：</span></span>
<span class="line"><span>  IP = 10.244.1.20</span></span>
<span class="line"><span>  Node = node-a</span></span>
<span class="line"><span></span></span>
<span class="line"><span>目标 Pod：</span></span>
<span class="line"><span>  IP = 10.244.2.31</span></span>
<span class="line"><span>  Node = node-b</span></span></code></pre></div><p>如果 restore 后 Pod IP 变了，原有连接的四元组也变了：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>源 IP:源端口 -&gt; 目标 IP:目标端口</span></span></code></pre></div><p>对端不会因为 Kubernetes 创建了一个新 Pod，就自动把旧连接迁移到新四元组上。</p><p>Service 只能影响新连接的负载均衡，通常不能拯救已经建立的连接。Ingress、Gateway、service mesh 也一样，它们可以帮助摘流和重建连接，但不能把任意 TCP 连接无损搬走。</p><p>因此更现实的定位是：</p><ol><li>checkpoint / restore 可以减少应用重新初始化的成本。</li><li>它不能保证长连接零中断。</li><li>跨节点迁移要么需要 CNI 支持 IP 保留和迁移，要么需要应用层重连。</li><li>对外服务要通过 readiness、drain、重试和幂等来兜底。</li></ol><h2 id="局限五-文件系统和卷状态不自动一致" tabindex="-1"><a class="header-anchor" href="#局限五-文件系统和卷状态不自动一致"><span>局限五：文件系统和卷状态不自动一致</span></a></h2><p>CRIU 保存的是进程视角下的一部分文件描述符和文件系统相关状态，不等于给整个 Pod 文件系统做了一次一致性快照。</p><p>Kubernetes 里常见几类存储：</p><table><thead><tr><th style="text-align:left;">存储类型</th><th style="text-align:left;">checkpoint / restore 风险</th></tr></thead><tbody><tr><td style="text-align:left;">容器可写层</td><td style="text-align:left;">checkpoint archive 可能包含部分 rootfs diff，但格式和行为依赖 runtime</td></tr><tr><td style="text-align:left;"><code>emptyDir</code></td><td style="text-align:left;">本地临时目录不会天然跨节点存在，需要额外复制或禁止依赖</td></tr><tr><td style="text-align:left;">PVC</td><td style="text-align:left;">需要目标节点能重新挂载，最好配合 VolumeSnapshot 做时间点一致性</td></tr><tr><td style="text-align:left;">hostPath / local PV</td><td style="text-align:left;">强绑定节点，跨节点 restore 很容易失败或读到不同数据</td></tr><tr><td style="text-align:left;">ConfigMap / Secret</td><td style="text-align:left;">restore 时内容可能已经更新，和 checkpoint 时的内存状态不一致</td></tr><tr><td style="text-align:left;">对象存储 / 数据库</td><td style="text-align:left;">不在 CRIU 管理范围内，需要应用自己保证事务和幂等</td></tr></tbody></table><p>最危险的情况是“内存回到了旧时间点，磁盘或远端状态却停在新时间点”。</p><p>例如一个任务在 checkpoint 后继续运行，并写入了一批结果文件。后来你拿旧 checkpoint restore，进程会以为那些写入还没发生，但文件系统里可能已经存在新结果。轻则重复计算，重则数据损坏。</p><p>CRIU 对 <code>--leave-running</code> 一类用法也有明确警告：如果 dump 后原进程继续运行，外部文件和连接继续变化，后续 restore 可能因为外部资源状态不匹配而失败或不一致。</p><p>所以生产里必须把 checkpoint 和存储快照作为一个一致性问题处理，而不能只看进程是否恢复成功。</p><h2 id="局限六-环境兼容性要求很高" tabindex="-1"><a class="header-anchor" href="#局限六-环境兼容性要求很高"><span>局限六：环境兼容性要求很高</span></a></h2><p>一个 checkpoint archive 不是通用镜像。它强依赖创建时的运行环境。</p><p>restore 目标节点最好满足：</p><ol><li>CPU 架构一致，例如都为 <code>amd64</code> 或都为 <code>arm64</code>。</li><li>CPU 特性兼容，例如 AVX、SSE、特定指令集不能缺失。</li><li>Linux kernel 版本和 namespace / cgroup 行为兼容。</li><li>cgroup v1 / v2 配置一致或被 runtime 正确适配。</li><li>CRIU、runc / crun、CRI-O / containerd 版本兼容。</li><li>SELinux、AppArmor、seccomp、capabilities 策略不会阻断 restore。</li><li>容器镜像、rootfs、挂载路径和外部依赖存在。</li><li>CNI、CSI、device plugin 行为一致。</li></ol><p>普通镜像启动失败时，通常还可以通过重新拉镜像、调整启动参数、重建 Pod 来恢复。checkpoint restore 失败时，失败点可能藏在内核对象、寄存器状态、FD、socket、namespace 或 runtime 内部，排障难度更高。</p><p>这也是为什么 checkpoint restore 在单机 demo 里容易成功，在异构生产集群中很难泛化。</p><h2 id="局限七-性能开销与内存大小强相关" tabindex="-1"><a class="header-anchor" href="#局限七-性能开销与内存大小强相关"><span>局限七：性能开销与内存大小强相关</span></a></h2><p>kubelet 官方文档明确提到，checkpoint 创建时间和容器已使用内存直接相关。原因很简单：checkpoint 需要把进程内存页写到磁盘。</p><p>影响 checkpoint 和 restore 性能的因素包括：</p><ol><li>进程实际使用的内存大小。</li><li>脏页比例。</li><li>进程数量和线程数量。</li><li>打开的文件描述符数量。</li><li>socket、pipe、IPC、epoll 等内核对象数量。</li><li>本地磁盘 IO 性能。</li><li>压缩算法和压缩率。</li><li>checkpoint archive 是否要上传到对象存储或 registry。</li><li>restore 前是否需要重新拉取大 checkpoint image。</li></ol><p>一个小型 busybox 计数器可能几百毫秒就能完成；一个几十 GiB 内存的 JVM、Notebook 或模型服务，checkpoint archive 可能非常大，写盘、压缩、上传、拉取和恢复都会成为明显成本。</p><p>这里有一个判断原则：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>如果 checkpoint image 拉取时间 + CRIU restore 时间</span></span>
<span class="line"><span>  &gt;= 应用普通冷启动时间</span></span>
<span class="line"><span>那么 checkpoint / restore 对冷启动优化的价值就很有限。</span></span></code></pre></div><p>它并不是总能更快。它只适合那些“初始化很贵，但运行态内存可接受，且外部状态可控”的 workload。</p><h2 id="局限八-安全风险很高" tabindex="-1"><a class="header-anchor" href="#局限八-安全风险很高"><span>局限八：安全风险很高</span></a></h2><p>checkpoint archive 通常包含进程内存。进程内存里可能有：</p><ol><li>Kubernetes ServiceAccount token。</li><li>TLS 私钥。</li><li>数据库密码。</li><li>JWT、OAuth token、session cookie。</li><li>用户请求内容。</li><li>明文业务缓存。</li><li>环境变量和配置。</li></ol><p>因此 checkpoint archive 的敏感级别通常高于普通容器镜像。</p><p>普通镜像一般包含二进制、依赖和配置模板；checkpoint archive 可能包含正在运行的业务秘密和用户数据。如果把 checkpoint archive 转成 OCI image 并推到 registry，本质上就是把内存 dump 推进了镜像仓库。</p><p>生产里至少要考虑：</p><ol><li>谁可以调用 kubelet checkpoint API。</li><li>谁可以登录节点读取 <code>/var/lib/kubelet/checkpoints</code>。</li><li>checkpoint archive 是否加密。</li><li>registry / 对象存储是否做了严格访问控制。</li><li>是否设置自动过期和 GC。</li><li>是否记录审计日志。</li><li>是否禁止对高敏 Pod 创建 checkpoint。</li></ol><p>不能因为它是“调试工具”或“平台能力”，就把它按普通日志或普通镜像处理。</p><h2 id="局限九-运维状态机缺失" tabindex="-1"><a class="header-anchor" href="#局限九-运维状态机缺失"><span>局限九：运维状态机缺失</span></a></h2><p>一次真实的 checkpoint / restore 通常不是一个命令，而是一串状态机：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>Draining</span></span>
<span class="line"><span>  -&gt; Checkpointing</span></span>
<span class="line"><span>  -&gt; Uploading</span></span>
<span class="line"><span>  -&gt; ImageBuilding</span></span>
<span class="line"><span>  -&gt; Pushing</span></span>
<span class="line"><span>  -&gt; Scheduling</span></span>
<span class="line"><span>  -&gt; Pulling</span></span>
<span class="line"><span>  -&gt; Restoring</span></span>
<span class="line"><span>  -&gt; Verifying</span></span>
<span class="line"><span>  -&gt; SwitchingTraffic</span></span>
<span class="line"><span>  -&gt; Cleaning</span></span></code></pre></div><p>每一步都可能失败：</p><table><thead><tr><th style="text-align:left;">阶段</th><th style="text-align:left;">可能失败原因</th></tr></thead><tbody><tr><td style="text-align:left;">Draining</td><td style="text-align:left;">Service / mesh 摘流超时，仍有请求进入</td></tr><tr><td style="text-align:left;">Checkpointing</td><td style="text-align:left;">CRIU 不支持某类资源，runtime 返回错误</td></tr><tr><td style="text-align:left;">Uploading</td><td style="text-align:left;">checkpoint archive 太大，网络或对象存储失败</td></tr><tr><td style="text-align:left;">ImageBuilding</td><td style="text-align:left;">checkpoint archive 格式和工具链不兼容</td></tr><tr><td style="text-align:left;">Scheduling</td><td style="text-align:left;">没有满足 kernel / runtime / CPU / 存储要求的节点</td></tr><tr><td style="text-align:left;">Pulling</td><td style="text-align:left;">registry 慢、权限失败、镜像层过大</td></tr><tr><td style="text-align:left;">Restoring</td><td style="text-align:left;">缺 IP、缺 mount、缺设备、seccomp/SELinux 阻断</td></tr><tr><td style="text-align:left;">Verifying</td><td style="text-align:left;">进程起来了，但业务健康检查失败</td></tr><tr><td style="text-align:left;">Cleaning</td><td style="text-align:left;">旧 Pod、旧 checkpoint image、临时文件没有清理</td></tr></tbody></table><p>Kubernetes 原生 checkpoint API 不负责这些状态。平台如果要把它做成稳定能力，就需要自研 Operator / Node Agent / 控制器状态机。</p><p>没有状态机时，最常见的问题是：demo 能跑通一次，但失败后不知道系统处在哪个阶段，也不知道哪些资源应该保留、重试或清理。</p><h2 id="常见失败现象" tabindex="-1"><a class="header-anchor" href="#常见失败现象"><span>常见失败现象</span></a></h2><table><thead><tr><th style="text-align:left;">现象</th><th style="text-align:left;">常见原因</th><th style="text-align:left;">排查方向</th></tr></thead><tbody><tr><td style="text-align:left;">kubelet 返回 404</td><td style="text-align:left;">feature gate 关闭，或 namespace / pod / container 不存在</td><td style="text-align:left;">查 kubelet 配置和目标 Pod 是否在本节点</td></tr><tr><td style="text-align:left;">kubelet 返回 500</td><td style="text-align:left;">CRI Runtime 没实现 checkpoint，或 CRIU dump 失败</td><td style="text-align:left;">查 kubelet、runtime、CRIU 日志</td></tr><tr><td style="text-align:left;">checkpoint archive 很大</td><td style="text-align:left;">进程内存大、脏页多、压缩率低</td><td style="text-align:left;">看 checkpointctl、内存用量和磁盘 IO</td></tr><tr><td style="text-align:left;">restore Pod 启动失败</td><td style="text-align:left;">checkpoint image annotation 不被 runtime 识别，或 runtime 版本不兼容</td><td style="text-align:left;">查镜像 manifest、containerd / CRI-O 版本</td></tr><tr><td style="text-align:left;">restore 时 CRIU 报 socket 错误</td><td style="text-align:left;">TCP / Unix socket 对端或网络状态不满足</td><td style="text-align:left;">检查连接、IP、CNI、iptables / eBPF</td></tr><tr><td style="text-align:left;">restore 后进程存在但业务异常</td><td style="text-align:left;">外部状态、文件系统、数据库、队列 offset 不一致</td><td style="text-align:left;">查业务幂等、事务、存储快照</td></tr><tr><td style="text-align:left;">多容器 Pod restore 异常</td><td style="text-align:left;">只恢复了业务容器，sidecar 状态不匹配</td><td style="text-align:left;">重新设计多容器一致性策略</td></tr><tr><td style="text-align:left;">checkpoint 文件泄漏</td><td style="text-align:left;">没有保留策略和 GC</td><td style="text-align:left;">加密、过期、审计和清理任务</td></tr></tbody></table><h2 id="哪些场景适合-哪些不适合" tabindex="-1"><a class="header-anchor" href="#哪些场景适合-哪些不适合"><span>哪些场景适合，哪些不适合</span></a></h2><p>比较适合先试点：</p><ol><li>单容器 Pod。</li><li>无 GPU、RDMA、DPDK、FUSE 等特殊设备。</li><li>外部连接少，或应用可以自动重连。</li><li>状态主要在进程内存中，且磁盘状态可控。</li><li>可以接受秒级或更长暂停。</li><li>有完善 readinessProbe 和业务健康检查。</li><li>目标是取证、实验、冷启动优化或低优先级任务恢复。</li></ol><p>不适合直接试点：</p><ol><li>多 sidecar、强依赖 service mesh 的业务 Pod。</li><li>网关、长连接服务、实时通信服务。</li><li>数据库主节点或强一致存储系统。</li><li>GPU 训练、RDMA、DPDK、eBPF 深度依赖 workload。</li><li>强依赖 <code>hostPath</code>、local PV 或节点本地缓存的 workload。</li><li>对 token、密钥、用户数据极其敏感且没有 checkpoint 加密方案的 workload。</li><li>不能接受重复执行、连接中断或短时间不可用的业务。</li></ol><h2 id="落地前的检查清单" tabindex="-1"><a class="header-anchor" href="#落地前的检查清单"><span>落地前的检查清单</span></a></h2><p>如果要在平台里尝试 Kubernetes 原生 checkpoint，建议先回答这些问题：</p><ol><li>目标是取证、冷启动优化、抢占恢复，还是迁移？</li><li>checkpoint 对象是单容器还是多容器 Pod？</li><li>restore 是否必须跨节点？</li><li>目标节点如何保证 kernel、runtime、CPU、cgroup 和安全策略兼容？</li><li>TCP 连接断开后，应用能否自动重连？</li><li>PVC、emptyDir、hostPath、本地缓存如何保证一致？</li><li>checkpoint archive 存到哪里，是否加密？</li><li>谁有权限创建、下载、restore checkpoint？</li><li>checkpoint image 和 archive 的 GC 策略是什么？</li><li>restore 成功的判定是进程存在，还是业务 readiness 通过？</li><li>restore 失败后旧 Pod 是否还在，如何回滚？</li><li>是否对目标 workload 做过多轮 checkpoint / restore 压测？</li></ol><p>如果这些问题没有答案，就不应该把它当成生产级热迁移能力。</p><h2 id="推荐定位" tabindex="-1"><a class="header-anchor" href="#推荐定位"><span>推荐定位</span></a></h2><p>更合理的定位是：</p><div class="language-text" data-highlighter="shiki" data-ext="text" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-text"><span class="line"><span>应用层 checkpoint：</span></span>
<span class="line"><span>  解决业务可靠恢复</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Kubernetes 原生 checkpoint + CRIU：</span></span>
<span class="line"><span>  解决进程现场保存和特定场景恢复</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Operator / Node Agent：</span></span>
<span class="line"><span>  解决声明式编排、状态机、存储、安全和调度</span></span>
<span class="line"><span></span></span>
<span class="line"><span>应用重连、幂等和事务：</span></span>
<span class="line"><span>  解决外部状态一致性</span></span></code></pre></div><p>也就是说，CRIU 路线不是应用层容错的替代品，而是补充能力。</p><p>如果目标是生产容灾，优先做应用层 checkpoint、事务、幂等和持久化状态恢复。如果目标是取证、冷启动优化、Notebook 休眠、低优先级任务抢占恢复，可以把 Kubernetes 原生 checkpoint 作为底层能力继续试点。</p><h2 id="总结" tabindex="-1"><a class="header-anchor" href="#总结"><span>总结</span></a></h2><p>Kubernetes 原生 checkpoint 的最大价值，是把 CRIU 的进程状态保存能力接入 kubelet，让平台可以在不进入容器内部的情况下保存运行现场。</p><p>但它的局限同样清晰：</p><ol><li>API 是 kubelet 节点级接口，不是声明式 Pod API。</li><li>操作对象是 container，不是完整 Pod。</li><li>原生接口只覆盖 checkpoint，不提供统一 restore 语义。</li><li>CRIU 不能保存所有设备、socket、文件锁、外部资源和硬件状态。</li><li>网络连接、存储状态、业务事务不自动一致。</li><li>restore 强依赖 kernel、runtime、CPU、cgroup 和安全策略兼容。</li><li>checkpoint archive 可能很大，性能收益需要实测。</li><li>checkpoint 文件包含内存数据，安全风险高。</li><li>平台化需要额外 Operator / Agent 状态机。</li></ol><p>所以它不是“开启 feature gate 就能拥有 Pod 热迁移”，而是一个底层能力。用得好，可以做取证、实验性恢复和特定 workload 优化；用错了，就会把进程恢复误认为业务恢复，把 checkpoint 文件误认为备份，把短暂停顿误认为零中断迁移。</p><h2 id="参考资料" tabindex="-1"><a class="header-anchor" href="#参考资料"><span>参考资料</span></a></h2><ol><li><a href="https://kubernetes.io/docs/reference/node/kubelet-checkpoint-api/" target="_blank" rel="noopener noreferrer">Kubernetes Kubelet Checkpoint API</a></li><li><a href="https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/" target="_blank" rel="noopener noreferrer">Kubernetes Feature Gates</a></li><li><a href="https://kubernetes.io/blog/2022/12/05/forensic-container-checkpointing-alpha/" target="_blank" rel="noopener noreferrer">Kubernetes Blog: Forensic container checkpointing in Kubernetes</a></li><li><a href="https://kubernetes.io/blog/2026/01/21/introducing-checkpoint-restore-wg/" target="_blank" rel="noopener noreferrer">Kubernetes Blog: Announcing the Checkpoint/Restore Working Group</a></li><li><a href="https://criu.org/Kubernetes" target="_blank" rel="noopener noreferrer">CRIU: Kubernetes</a></li><li><a href="https://www.criu.org/index.php?title=What_cannot_be_checkpointed" target="_blank" rel="noopener noreferrer">CRIU: What cannot be checkpointed</a></li><li><a href="https://criu.org/Advanced_usage" target="_blank" rel="noopener noreferrer">CRIU: Advanced usage</a></li><li><a href="https://www.criu.org/Live_migration" target="_blank" rel="noopener noreferrer">CRIU: Live migration</a></li></ol>`,112)])])}const c=t(n,[["render",l]]),o=JSON.parse('{"path":"/k8s/k8s-checkpoint-limitations/","title":"Kubernetes 原生 Checkpoint 的局限性：为什么它不是通用 Pod 热迁移","lang":"zh-CN","frontmatter":{"title":"Kubernetes 原生 Checkpoint 的局限性：为什么它不是通用 Pod 热迁移","createTime":"2026/05/29 12:00:00","permalink":"/k8s/k8s-checkpoint-limitations/","description":"一句话理解 Kubernetes 原生 checkpoint 解决的是“把某个正在运行的容器运行现场保存下来”的问题，而不是完整解决“Pod 可靠恢复”“业务容灾”或“零中断迁移”的问题。 它的底层依赖 CRIU，因此能力边界也继承了 CRIU 的边界：CRIU 能保存很多 Linux 进程状态，比如内存页、寄存器、文件描述符、进程树、namespac...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Kubernetes 原生 Checkpoint 的局限性：为什么它不是通用 Pod 热迁移\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-05-29T13:21:51.000Z\\",\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://blog.jianzhihao.icu/k8s/k8s-checkpoint-limitations/"}],["meta",{"property":"og:site_name","content":"zhihaoの博客"}],["meta",{"property":"og:title","content":"Kubernetes 原生 Checkpoint 的局限性：为什么它不是通用 Pod 热迁移"}],["meta",{"property":"og:description","content":"一句话理解 Kubernetes 原生 checkpoint 解决的是“把某个正在运行的容器运行现场保存下来”的问题，而不是完整解决“Pod 可靠恢复”“业务容灾”或“零中断迁移”的问题。 它的底层依赖 CRIU，因此能力边界也继承了 CRIU 的边界：CRIU 能保存很多 Linux 进程状态，比如内存页、寄存器、文件描述符、进程树、namespac..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-05-29T13:21:51.000Z"}],["meta",{"property":"article:modified_time","content":"2026-05-29T13:21:51.000Z"}]]},"readingTime":{"minutes":15.93,"words":4779},"git":{"createdTime":1780060911000,"updatedTime":1780060911000,"contributors":[{"name":"PersistentJZH","username":"PersistentJZH","email":"zhihao.kan17@gmail.com","commits":1,"avatar":"https://avatars.githubusercontent.com/PersistentJZH?v=4","url":"https://github.com/PersistentJZH"}]},"autoDesc":true,"filePathRelative":"notes/kubernetes/4. k8s增强/openkruise/agent/checkpoint-limitations.md","headers":[]}');export{c as comp,o as data};
