---
title: docker网络
mathjax: true
thumbnail: false
cover: 容器网络-20240809150306878.webp
categories:
  - 后端
  - linux
  - 网络
  - 容器网络
date: 2024-08-09 14:30:14
updated: 2024-08-09 14:30:14
tags:
---

# docker网络
![](docker网络-20240809143044718.webp)

在宿主机上会多一张docker的网桥
```bash
[root@iZ2vcfvp9613lxljhuxu45Z ~]# ifconfig

docker0: flags=4163<UP,BROADCAST,RUNNING,MULTICAST>  mtu 1500
        inet 172.17.0.1  netmask 255.255.0.0  broadcast 172.17.255.255
        inet6 fe80::42:d4ff:fe90:8388  prefixlen 64  scopeid 0x20<link>
        ether 02:42:d4:90:83:88  txqueuelen 0  (Ethernet)
        RX packets 612816103  bytes 495065789920 (461.0 GiB)
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 624966472  bytes 615335282581 (573.0 GiB)
        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0
        
veth128588e: flags=4163<UP,BROADCAST,RUNNING,MULTICAST>  mtu 1500
        inet6 fe80::bc7f:42ff:fe26:73c6  prefixlen 64  scopeid 0x20<link>
        ether be:7f:42:26:73:c6  txqueuelen 0  (Ethernet)
        RX packets 17959  bytes 13915271 (13.2 MiB)
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 14176  bytes 3480391 (3.3 MiB)
        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0

veth6deb23d: flags=4163<UP,BROADCAST,RUNNING,MULTICAST>  mtu 1500
        inet6 fe80::45f:57ff:feb8:54db  prefixlen 64  scopeid 0x20<link>
        ether 06:5f:57:b8:54:db  txqueuelen 0  (Ethernet)
        RX packets 95  bytes 20587 (20.1 KiB)
        RX errors 0  dropped 0  overruns 0  frame 0
        TX packets 133  bytes 13070 (12.7 KiB)
        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0
```



```
[root@iZ2vcfvp9613lxljhuxu45Z ~]# brctl show
bridge name	bridge id		STP enabled	interfaces
br-e15b91b3196a		8000.0242204c4fd5	no		veth128588e
							veth8d2a418
							veth8e3a63c
							veth9fec062
							vetha5a80be
							vethe6352b7
docker0		8000.0242d4908388	no		veth6deb23d
```



docker网桥的使用

```bash
# 创建网桥
[root@iZ2vcfvp9613lxljhuxu45Z ~]# docker network create -d bridge jzh-net
ce31941bcbea1092be1010f47c330e268a5ce13cd4167385dd9c871ad05b22a8

# 查看网桥
docker network ls
[root@iZ2vcfvp9613lxljhuxu45Z ~]# docker network ls
NETWORK ID     NAME                   DRIVER    SCOPE
5e87ec119ea1   bridge                 bridge    local
e15b91b3196a   docker-apisix_apisix   bridge    local
d1094c6a6aa2   host                   host      local
28b263e4b327   jzh-bridge             bridge    local
ce31941bcbea   jzh-net                bridge    local
f39e6e2fb083   none                   null      local

# 查看网桥接口
brctl show
[root@iZ2vcfvp9613lxljhuxu45Z ~]# brctl show
bridge name	bridge id		STP enabled	interfaces
br-28b263e4b327		8000.0242bf9ab554	no		vethc4738fa
br-ce31941bcbea		8000.0242aec4a505	no
br-e15b91b3196a		8000.0242204c4fd5	no		veth128588e
							veth8d2a418
							veth8e3a63c
							veth9fec062
							vetha5a80be
							vethe6352b7
docker0		8000.0242d4908388	no		veth6deb23d

# 创建一个容器，让容器加入到新的网络之中
[root@iZ2vcfvp9613lxljhuxu45Z ~]# docker container run -d --name demo2 --network jzh-net alpine sleep 3600
d3dc581c0fd1b7865291769e8e95699540538de3545f4a50e1a01a0eae40613c
[root@iZ2vcfvp9613lxljhuxu45Z ~]# brctl show
bridge name	bridge id		STP enabled	interfaces
br-28b263e4b327		8000.0242bf9ab554	no		vethc4738fa
br-ce31941bcbea		8000.0242aec4a505	no		veth2dbe03e
br-e15b91b3196a		8000.0242204c4fd5	no		veth128588e
							veth8d2a418
							veth8e3a63c
							veth9fec062
							vetha5a80be
							vethe6352b7
docker0		8000.0242d4908388	no		veth6deb23d

```
如果在相同的网络中继续接入新的容器，那么新接入的容器是可以通过 demo1 这个名称来 ping 通的。
![](docker网络-20240809145224329.webp)




# kubernates网络
## 同 pod 内的容器间通信
同 pod 内的容器间通信最简单，这些容器共享网络命名空间，每个命名空间下都有 `lo` 回环接口，可以通过 `localhost` 来完成通信。
![](容器网络-20240809150034999.webp)
## 同节点上的pod之间通信
和docker类似 都是通过网桥来实现
当我们将 `curl` 容器和 `httpbin` 分别在两个 pod 中运行，这两个 pod 有可能调度到同一个节点上。`curl` 发出的请求根据容器内的路由表到达了 pod 内的 `eth0` 接口。然后通过与 `eth0` 相连的隧道 `veth1` 到达节点的根网络空间。

`veth1` 通过网桥 `cni0` 与其他 pod 相连虚拟以太接口 `vethX` 相连，网桥会询问所有相连的接口是否拥有原始请求中的 IP 地址（比如这里的 `10.42.1.9`）。收到响应后，网桥会记录映射信息（`10.42.1.9` => `veth0`），同时将数据转发过去。最终数据经过 `veth0` 隧道进入 pod `httpbin` 中。
![](容器网络-20240809150107587.webp)


## 不同节点间的pod通信（不同的插件来解决）
跨节点的 pod 间通信会复杂一些，且 **不同网络插件的处理方式不同**，这里选择一种容易理解的方式来简单说明下。

前半部分的流程与同节点 pod 间通信类似，当请求到达网桥，网桥询问哪个 pod 拥有该 IP 但是没有得到回应。流程进入主机的路由寻址过程，到更高的集群层面。

在集群层面有一张路由表，里面存储着每个节点的 Pod IP 网段（节点加入到集群时会分配一个 Pod 网段（Pod CIDR），比如在 k3s 中默认的 Pod CIDR 是 `10.42.0.0/16`，节点获取到的网段是 `10.42.0.0/24`、`10.42.1.0/24`、`10.42.2.0/24`，依次类推）。通过节点的 Pod IP 网段可以判断出请求 IP 的节点，然后请求被发送到该节点。
![](容器网络-20240809150306878.webp)

## pod的ip分配
在通过 kube-controller-manager 组件的 `--cluster-cidr` 控制了 Pod IP 的 CIDR 网段后，首先会在集群中每个 Node 分配一个 subnet CIDR，他们都属于 `--cluster-cidr` 网段。

```
root@master:/home/ubuntu# kubectl get nodes -o jsonpath='{range .items[*]}{.metadata.name} {.spec.podCIDR}{"\n"}{end}'
worker2 10.42.1.0/24
worker1 10.42.2.0/24
master 10.42.0.0/24
```

## kubernates网络插件
### flannel
![](容器网络-20240809151849918.webp)
当发送到 `10.42.1.2` 流量到达节点 A 的网桥 `cni0`，由于目标 IP 并不属于当前阶段的网段。根据系统的路由规则，进入到接口 `flannel.1`，也就是 VXLAN 的 vtep。这里的路由规则也由 `flanneld` 来维护，当节点上线或者下线时，都会更新路由规则。
`flannel.1` 将原始的以太封包使用 UDP 协议重新封装，将其发送到目标地址 `10.42.1.0` （目标的 MAC 地址通过 ARP 获取）。对端的 vtep 也就是 `flannel.1` 的 UDP 端口 8472 收到消息，解帧出以太封包，然后对以太封包进行路由处理，发送到接口 `cni0`，最终到达目标 pod 中。

响应的数据传输与请求的处理也是类似，只是源地址和目的地址调换。

```
root@master:/home/ubuntu# ip route
default via 192.168.64.1 dev enp0s1 proto dhcp src 192.168.64.4 metric 100
10.42.0.0/24 dev cni0 proto kernel scope link src 10.42.0.1
10.42.1.0/24 via 10.42.1.0 dev flannel.1 onlink
10.42.2.0/24 via 10.42.2.0 dev flannel.1 onlink
192.168.64.0/24 dev enp0s1 proto kernel scope link src 192.168.64.4
192.168.64.1 dev enp0s1 proto dhcp scope link src 192.168.64.4 metric 100
```


### Cilium
![](容器网络-20240809153322359.webp)

